<h1>Books</h1>
<table>
  <head>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
    <% @books.each do |book| %>

      <%= image_tag @user.get_profile_image(100,100) %>
      <%= link_to book_path(book.id) do %>

    <% end %>
  </head>
</table>