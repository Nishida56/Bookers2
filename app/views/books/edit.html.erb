<h3>Books#edit　本の中身編集できるよ</h3>

<h1>Editing Book</h1>
  <%= form_with model: @book do |f| %>
    <p>Title</p>
    <%= f.text_field :title %>
    <p>Opinion</p>
    <%= f.text_area :body %><br>
    <%= f.submit "Update Book" %>
  <% end %>
<%= link_to 'show', book_path %>|<%= link_to 'back', books_path %>